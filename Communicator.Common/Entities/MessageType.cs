namespace Communicator.Common.Entities
{
    public enum MessageType
    {
        ClientConnected,
        ClientDisconnected,
        PrivateMessage,
        Global,
        Init
    }
}
